w_userflow_featurestore:
  target: dev
  outputs:
    dev:
      type: spark
      method: thrift
      host: spark-thrift-server
      port: 10000
      schema: bronze
      auth: NOSASL
      threads: 4
      connect_timeout: 30
      retries: 3
      use_ssl: false
